# M4 界面与交互约定

原生 Godot 游戏 UI，使用现有工业黄 #E9C879、深灰 #1D262C、钢灰 #414B51、白 #EDF0E5。系统字体：标题 26、主要金额 26、按钮 18、说明 15。保持 1280×720 基准与 canvas_items 缩放。
顶部：金币、Pressure Level、最大压力。左侧：当前物品、材质、负载、峰值/当前压缩。右侧：选择/结算/升级，按状态切换。底部：PRESS/PAUSE、RETURN、NEW OBJECT、COLLECT、UPGRADES、声音。
醒目的一条下一步提示贯穿选择→压缩→回程→领取→升级；免费生成、升级价格与轮胎解锁条件直接写在游戏内。
按钮有键盘焦点；不能执行的操作显示禁用并在提示中解释。结算后一次性领取；保存失败不显示已到账。简单短暂解锁提示，不制作复杂动画。
权威状态：gameplay.gd 管当前物品生命周期，progress.gd 管金币/升级/存档，旧 main.gd 与材料脚本管已验收机器和形变。M4 不修改旧材料实现。

# M5 视觉与交互
主体：小型液压回收车间，玩家看清材料被压坏并体验机器成长。机器居中偏左，右侧交互台，顶部轻量钱包，底部一个主操作。商业感来自金属、混凝土、实用灯具与精确的动作节奏。
色板：煤黑 #11181B、钢灰 #35434B、工业黄 #E3B84B、浅纸白 #E7E6DC、状态绿 #A5C58E。Godot 系统字体使用粗体显示金额/标题、常规体说明、等宽字体调试信息。
UI 权威 owner：slice_hud.gd（原生 Godot Button/PanelContainer、选择/结算/升级/解锁/反馈）。gameplay.gd 仍是生命周期来源，progress.gd 是持久经济来源；hud 不修改金币事实。Debug 默认隐藏，F3 开关。
反馈：收集成功0.65秒数字递增、+COINS短幅上移；升级0.8秒机械确认与灯光脉冲，独立解锁页展示轮胎轮廓。主按钮黄，次按钮钢灰，其余低对比或隐藏。保存失败不播放到账反馈。
环境 owner：workshop.gd；只加车间表面、管线、标志和灯具。低频细节纹理由固定随机种子生成，图片与素材均原创；不过度加实时阴影。HUD 缩放随 1280×720 canvas_items，无网页框架/DOM。验收采用 Windows 实际渲染、按键/按钮/场景测试，web audit 仅可检查适用的设计文档。
## M6 目录扩展

沿用 M5 玩家 HUD / Debug HUD 与视觉语言。`slice_hud.gd` 继续唯一负责界面表现，七物品选择使用固定高度滚动列表；压力、费用和解锁文本取自 `progress.gd`。新增结构几何不改已有三材质。升级反馈仍为升级 → 独立解锁 → 立即尝试。
